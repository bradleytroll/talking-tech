{{!-- <h1>All Posts</h1>
<div class="posts list-group">
    {{#each posts}}
    <div class="list-group-item">
        <h2>{{this.title}}</h2>
        <p>{{this.content}}</p>
        <p>Posted by {{this.user.username}} on {{this.createdAt}}</p>
        <a href="/posts/{{this._id}}">View Post</a>
    </div>
    {{/each}}
</div> --}}