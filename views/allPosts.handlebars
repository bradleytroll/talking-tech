<div class='posts'>
    {{#each posts}}
    <div class='card'>
        <h2>{{this.title}}</h2>
        <p>{{this.content}}</p>
        <p>Posted by {{this.user.username}} on {{this.createdAt}}</p>
    </div>
    {{else}}
    <p>No posts to display</p>
    {{/each}}       
</div>